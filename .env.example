MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/macandglow?retryWrites=true&w=majority
# IMPORTANT: If your password contains special characters like @, #, $, /, etc.,
# they must be URL-encoded. For @ use %40, for # use %23, for $ use %24, etc.

JWT_SECRET=your_super_secret_key_here_change_this

# Admin credentials (create via seed.js):
# Email: admin@macandglow.com
# Password: admin123
